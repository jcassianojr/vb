   sTMPFILE = OpenArqExt(Me, "", "JPG", "JPEG *.JPG")
     If Len(sTMPFILE) > 0 Then
        If FixInt(FileLen(sTMPFILE)) > 500000 Then
           Alert ("Imagem Muito Grande,Ajuste o tamanho")
           If Not MDG("Anexar mesmo assim-NAO RECOMENDADO") Then
              Exit Sub
           End If
        End If
        Picture1.Picture = LoadPictureEx(sTMPFILE)
        StretchSourcePictureFromFile sTMPFILE, Picture2
        lTROCOU = True
     End If
	 

     sTMPFILE = OpenArqExt(Me, "", "JPG", "JPEG *.JPG")
     If Len(sTMPFILE) > 0 Then
        If FixInt(FileLen(sTMPFILE)) > 500000 Then
           Alert ("Imagem Muito Grande,Ajuste o tamanho")
           If Not MDG("Anexar mesmo assim-NAO RECOMENDADO") Then
              Exit Sub
           End If
        End If
        Picture1(Index).Picture = LoadPictureEx(sTMPFILE)
        StretchSourcePictureFromFile sTMPFILE, Picture2(Index)
        lTROCOU(Index) = True
     End If	 